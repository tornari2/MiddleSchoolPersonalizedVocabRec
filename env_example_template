# OpenAI Configuration for Vocabulary Recommendation Engine
# Get your API key from: https://platform.openai.com/api-keys
# NEVER commit your actual .env file to version control!

# ==========================================
# REQUIRED: OpenAI API Key
# ==========================================
# Replace 'your_openai_api_key_here' with your actual OpenAI API key
# Your key should start with 'sk-' and be approximately 51 characters long
OPENAI_API_KEY=your_openai_api_key_here

# ==========================================
# OPTIONAL: OpenAI Model Configuration
# ==========================================

# Model to use for text generation and recommendations
# Options: gpt-4o-mini, gpt-3.5-turbo, gpt-4, gpt-4-turbo-preview
# Default: gpt-4o-mini (excellent balance of performance and cost for recommendations)
OPENAI_MODEL=gpt-4o-mini

# Temperature for text generation (0.0 = deterministic, 1.0 = very creative)
# For recommendations: Use lower values (0.1-0.3) for consistent, reliable suggestions
# Default: 0.2
OPENAI_TEMPERATURE=0.2

# Maximum tokens for recommendation generation
# Higher values allow more detailed recommendations but cost more
# Default: 500 (sufficient for vocabulary recommendations)
OPENAI_MAX_TOKENS=500

# ==========================================
# OPTIONAL: Advanced OpenAI Settings
# ==========================================

# Top-p sampling (nucleus sampling) - alternative to temperature
# Default: 0.9 (good balance for recommendations)
OPENAI_TOP_P=0.9

# Frequency penalty (-2.0 to 2.0)
# Reduces repetition of frequent tokens
# Default: 0.0 (no penalty)
OPENAI_FREQUENCY_PENALTY=0.0

# Presence penalty (-2.0 to 2.0)
# Encourages new topics/concept introduction
# Default: 0.0 (no penalty)
OPENAI_PRESENCE_PENALTY=0.0

# ==========================================
# RECOMMENDATION ENGINE CONFIGURATION
# ==========================================

# Enable OpenAI-powered recommendations
# Set to true to use AI for enhanced vocabulary recommendations
# Default: false (use traditional algorithm)
USE_OPENAI_RECOMMENDATIONS=false

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and replace 'your_openai_api_key_here' with your actual API key
#
# 3. For development/testing:
#    python test_openai_integration.py
#
# 4. For production use in Lambda:
#    The .env file is for local development only.
#    For AWS Lambda, use AWS Secrets Manager (see terraform config)
#
# SECURITY NOTES:
# - The .env file is automatically ignored by .gitignore
# - Never share your .env file or commit it to version control
# - Rotate your API key regularly for security
# - Use different keys for development/staging/production
